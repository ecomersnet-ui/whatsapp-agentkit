{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "uvicorn agent.main:app --host 0.0.0.0 --port 8000",
    "restartPolicyType": "always",
    "restartPolicyMaxRetries": 5
  }
}
